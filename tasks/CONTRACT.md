# Shared contract
Only work within /home/flori/Dev/chutes/philosophen. Never modify chutes-web or unrelated apps. All work on Sandy. Secrets environment only; never print values. No deployments, git commits or Telegram sends by workers. Coordinator does those.
Stack: Vite React plain JSX, Express Node ESM. package.json coordinator owned. Server at server/index.js exports app, listens PORT=3000; serves dist in production. UI entry src/main.jsx; index.html root. Public assets /portraits/{sokrates,aristoteles,epikur,kant,nietzsche,arendt,beauvoir,camus}.webp. Brand ivory #f5f1e8, ink #192c29, terracotta #b6573d, antique gold. Premium literary/philosophical salon. German all UI.
GET /api/personas => {personas:[{id,name,dates,worldview,greeting,starters:[],image}]} no system prompt exposure.
POST /api/chat JSON {philosopher:id,messages:[{role:'user'|'assistant',content:string}],version:string} => {reply,model,version}. Nonstream responsive loading is acceptable. Errors {error,code,version} appropriate HTTP. X-App-Version response header. Version mismatch 409 code VERSION_MISMATCH. GET /api/version no-store => {version}. GET /healthz => {ok:true,version}.
POST /api/transcribe multipart audio => {text}; Groq whisper-large-v3, editable transcript in UI, never autosend. Optional version form field.
Build version injected through Vite define __APP_VERSION__ and server reads version.json. Coordinator builds scripts/build.mjs. SW generated as dist/sw.js by build script replacing __BUILD_VERSION__ in public/sw.js. Manifest /manifest.webmanifest. All API and HTML no-store; SW no-cache; hashed assets immutable. UI save drafts before safe update, never reload busy chat/record/transcribe. Registration update on foreground; no API caching. Offline graceful.
