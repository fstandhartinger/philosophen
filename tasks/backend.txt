Implement complete backend; own ONLY server/ and tests/backend.test.js. Read tasks/CONTRACT.md. Node ESM Express. Export createApp with injectable fetch/model settings for supertest mocks and app. Production static dist; version.json. Eight substantive distinctive historically grounded German persona system prompts (150+ words each), worldview/greeting/starters metadata. Input 24 messages max 12000 total chars 4000 per message, strict alternating roles no system. Secure public endpoints, helmet compatible local fonts, same-origin check for POST, bounded in-memory per-IP + global daily limits, global concurrency limit. proxy trust appropriate private Coolify hop; never blind trust spoofable first XFF. Rate limit transcribe separately. No user content or keys logs. Chat fallback exactly Chutes moonshotai/Kimi-K3-TEE -> Chutes Qwen/Qwen3.8-27B-TEE -> OpenRouter openrouter/free -> OpenRouter openai/gpt-5.6-luna. All IDs verified live catalog by coordinator. Env keys CHUTES_API_KEY OPENROUTER_API_KEY GROQ_API_KEY. Per attempt timeout 25s, total 100s, errors/empty/timeout next. Kimi nonthinking chat_template_kwargs enable_thinking false if supported; enough max_tokens ~1400. Do not expose reasoning. Robust Groq whisper-large-v3 multipart memory bounded 12MB mime allow list webm/mp4/ogg/wav/mpeg with 35sec timeout. API version mismatch, no-store, SW no-cache hashed immutable. Meaningful deterministic tests verify exact fallback incl empty/error/timeouts, limits, missing versions, malformed bodies, transcription errors, XSS-safe server behavior. Run tests once npm dependencies present. No deployment. Write evidence/backend-worker.md with work and test results.
